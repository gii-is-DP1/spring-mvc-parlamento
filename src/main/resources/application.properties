# ACCESO A DATOS:
# MYSQL
#spring.datasource.url=jdbc:mysql://localhost:3306/parlamento
#spring.datasource.username=alumno
#spring.datasource.password=alumnoalumno
 #spring.datasource.driver-class-name=com.mysql.jdbc.Driver
# H2
spring.h2.console.enabled=true
# Necesario para soportar nombres de tabla en mayuscula completamente
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
# Hibernate ddl auto (create, create-drop, update, none): with "update" the database
# schema will be automatically updated accordingly to java entities found in the project
spring.jpa.hibernate.ddl-auto = update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# VISTA
spring.mvc.view.prefix: /WEB-INF/views/
spring.mvc.view.suffix: .jsp
# DATA REST API CONFIG:
spring.data.rest.basePath=/data-api
spring.data.rest.defaultMediaType=application/json
jwt.secret=grandenawer